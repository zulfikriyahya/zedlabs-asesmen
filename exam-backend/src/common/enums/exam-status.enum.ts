// ── exam-status.enum.ts ──────────────────────────────────────────────────────
export enum ExamPackageStatus {
  DRAFT = 'DRAFT',
  REVIEW = 'REVIEW',
  PUBLISHED = 'PUBLISHED',
  ARCHIVED = 'ARCHIVED',
}
